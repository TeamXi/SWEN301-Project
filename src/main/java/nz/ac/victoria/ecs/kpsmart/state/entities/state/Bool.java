package nz.ac.victoria.ecs.kpsmart.state.entities.state;

/**
 * NASTY HACK - Derby does not support a native boolean type, so we use an enum to fudge it.
 * 
 * @author hodderdani
 *
 */
public enum Bool {
	True,
	False
}
